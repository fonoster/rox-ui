// import { h } from 'preact'

// import { SpeechContext } from './SpeechContext'

export interface SpeechProviderProps {}

/**
 * The provider for Speech
 *
 * @description Make sure you have only one SpeechProvider in your application,
 * because otherwise the audio will be mixed up and unusable.
 *
 * It is possible to switch the props on the fly, which will make provider stop current client if it's running
 * and start a new one.
 *
 * @author Fonoster
 */
export const SpeechProvider = () => {
  // return <SpeechContext.Provider value={{}} />
}
